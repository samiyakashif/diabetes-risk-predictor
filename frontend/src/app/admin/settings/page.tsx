export default function AdminSettingsPage() {
  return (
    <div>
      <h2 className="text-xl font-bold text-foreground" style={{ fontFamily: "var(--font-display)" }}>
        Admin Settings
      </h2>
      <p className="text-muted-foreground mt-2">
        System settings — to be built in Step 5.
      </p>
    </div>
  );
}